///
/// ScannerTrigger.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::bleprintandscan {

  /**
   * An enum which can be represented as a JavaScript enum (ScannerTrigger).
   */
  enum class ScannerTrigger {
    SOFT_TRIGGER_1S      SWIFT_NAME(softTrigger1s) = 1,
    SOFT_TRIGGER_2S      SWIFT_NAME(softTrigger2s) = 2,
    SOFT_TRIGGER_3S      SWIFT_NAME(softTrigger3s) = 3,
    SOFT_TRIGGER_4S      SWIFT_NAME(softTrigger4s) = 4,
    SOFT_TRIGGER_5S      SWIFT_NAME(softTrigger5s) = 5,
    SOFT_TRIGGER_6S      SWIFT_NAME(softTrigger6s) = 6,
    SOFT_TRIGGER_7S      SWIFT_NAME(softTrigger7s) = 7,
  } CLOSED_ENUM;

} // namespace margelo::nitro::bleprintandscan

namespace margelo::nitro {

  using namespace margelo::nitro::bleprintandscan;

  // C++ ScannerTrigger <> JS ScannerTrigger (enum)
  template <>
  struct JSIConverter<ScannerTrigger> final {
    static inline ScannerTrigger fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<ScannerTrigger>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, ScannerTrigger arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double number = value.getNumber();
      int integer = static_cast<int>(number);
      if (number != integer) {
        // The integer is not the same value as the double - we truncated floating points.
        // Enums are all integers, so the input floating point number is obviously invalid.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 1 && integer <= 7;
    }
  };

} // namespace margelo::nitro
