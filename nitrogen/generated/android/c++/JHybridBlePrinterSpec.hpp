///
/// HybridBlePrinterSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#include <NitroModules/JHybridObject.hpp>
#include <fbjni/fbjni.h>
#include "HybridBlePrinterSpec.hpp"




namespace margelo::nitro::bleprintandscan {

  using namespace facebook;

  class JHybridBlePrinterSpec: public jni::HybridClass<JHybridBlePrinterSpec, JHybridObject>,
                               public virtual HybridBlePrinterSpec {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/bleprintandscan/HybridBlePrinterSpec;";
    static jni::local_ref<jhybriddata> initHybrid(jni::alias_ref<jhybridobject> jThis);
    static void registerNatives();

  protected:
    // C++ constructor (called from Java via `initHybrid()`)
    explicit JHybridBlePrinterSpec(jni::alias_ref<jhybridobject> jThis) :
      HybridObject(HybridBlePrinterSpec::TAG),
      _javaPart(jni::make_global(jThis)) {}

  public:
    ~JHybridBlePrinterSpec() override {
      // Hermes GC can destroy JS objects on a non-JNI Thread.
      jni::ThreadScope::WithClassLoader([&] { _javaPart.reset(); });
    }

  public:
    size_t getExternalMemorySize() noexcept override;
    void dispose() noexcept override;

  public:
    inline const jni::global_ref<JHybridBlePrinterSpec::javaobject>& getJavaPart() const noexcept {
      return _javaPart;
    }

  public:
    // Properties
    

  public:
    // Methods
    std::shared_ptr<Promise<void>> initializePrinter() override;
    std::shared_ptr<Promise<void>> startScanningForPrinters(const std::function<void(const std::vector<Device>& /* devices */)>& onDeviceFound) override;
    std::shared_ptr<Promise<void>> suspendScanForPrinters() override;
    std::shared_ptr<Promise<void>> connectToPrinter(const std::string& deviceId) override;
    std::shared_ptr<Promise<void>> disconnectFromPrinter(const std::string& deviceId) override;
    std::shared_ptr<Promise<bool>> isPrinterConnected(const std::string& deviceId) override;
    std::shared_ptr<Promise<std::vector<Device>>> getConnectedPrinters() override;
    std::shared_ptr<Promise<void>> disconnectAllPrinters() override;
    std::shared_ptr<Promise<std::vector<std::shared_ptr<ArrayBuffer>>>> generateBytecode(const std::string& value, double printerWidth, double mtuSize) override;
    std::shared_ptr<Promise<std::vector<std::string>>> generateBytecodeBase64(const std::string& value, double printerWidth, double mtuSize) override;
    std::shared_ptr<Promise<void>> sendToBluetoothThermalPrinter(const std::string& deviceId, const std::string& value, double printerWidth) override;
    std::shared_ptr<Promise<void>> sendToUsbThermalPrinter(const std::string& value, double printerWidth, double chunkSize) override;
    std::shared_ptr<Promise<void>> feedPaper(const std::string& deviceId, double lines) override;
    std::shared_ptr<Promise<void>> cutPaper(const std::string& deviceId) override;

  private:
    friend HybridBase;
    using HybridBase::HybridBase;
    jni::global_ref<JHybridBlePrinterSpec::javaobject> _javaPart;
  };

} // namespace margelo::nitro::bleprintandscan
