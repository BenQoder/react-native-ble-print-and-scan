///
/// HybridBlePrinterSpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#include "HybridBlePrinterSpec.hpp"

// Forward declaration of `HybridBlePrinterSpec_cxx` to properly resolve imports.
namespace BlePrintAndScan { class HybridBlePrinterSpec_cxx; }

// Forward declaration of `Device` to properly resolve imports.
namespace margelo::nitro::bleprintandscan { struct Device; }
// Forward declaration of `ArrayBuffer` to properly resolve imports.
namespace NitroModules { class ArrayBuffer; }
// Forward declaration of `ArrayBufferHolder` to properly resolve imports.
namespace NitroModules { class ArrayBufferHolder; }

#include <NitroModules/Promise.hpp>
#include "Device.hpp"
#include <vector>
#include <functional>
#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include <NitroModules/ArrayBufferHolder.hpp>

#include "BlePrintAndScan-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::bleprintandscan {

  /**
   * The C++ part of HybridBlePrinterSpec_cxx.swift.
   *
   * HybridBlePrinterSpecSwift (C++) accesses HybridBlePrinterSpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridBlePrinterSpec_cxx can directly inherit from the C++ class HybridBlePrinterSpec
   * to simplify the whole structure and memory management.
   */
  class HybridBlePrinterSpecSwift: public virtual HybridBlePrinterSpec {
  public:
    // Constructor from a Swift instance
    explicit HybridBlePrinterSpecSwift(const BlePrintAndScan::HybridBlePrinterSpec_cxx& swiftPart):
      HybridObject(HybridBlePrinterSpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline BlePrintAndScan::HybridBlePrinterSpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }

  public:
    // Properties
    

  public:
    // Methods
    inline std::shared_ptr<Promise<void>> initializePrinter() override {
      auto __result = _swiftPart.initializePrinter();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> startScanningForPrinters(const std::function<void(const std::vector<Device>& /* devices */)>& onDeviceFound) override {
      auto __result = _swiftPart.startScanningForPrinters(onDeviceFound);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> suspendScanForPrinters() override {
      auto __result = _swiftPart.suspendScanForPrinters();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> connectToPrinter(const std::string& deviceId) override {
      auto __result = _swiftPart.connectToPrinter(deviceId);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> disconnectFromPrinter(const std::string& deviceId) override {
      auto __result = _swiftPart.disconnectFromPrinter(deviceId);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<bool>> isPrinterConnected(const std::string& deviceId) override {
      auto __result = _swiftPart.isPrinterConnected(deviceId);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<std::vector<Device>>> getConnectedPrinters() override {
      auto __result = _swiftPart.getConnectedPrinters();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> disconnectAllPrinters() override {
      auto __result = _swiftPart.disconnectAllPrinters();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<std::vector<std::shared_ptr<ArrayBuffer>>>> generateBytecode(const std::string& value, double printerWidth, double mtuSize) override {
      auto __result = _swiftPart.generateBytecode(value, std::forward<decltype(printerWidth)>(printerWidth), std::forward<decltype(mtuSize)>(mtuSize));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<std::vector<std::string>>> generateBytecodeBase64(const std::string& value, double printerWidth, double mtuSize) override {
      auto __result = _swiftPart.generateBytecodeBase64(value, std::forward<decltype(printerWidth)>(printerWidth), std::forward<decltype(mtuSize)>(mtuSize));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> sendToBluetoothThermalPrinter(const std::string& deviceId, const std::string& value, double printerWidth) override {
      auto __result = _swiftPart.sendToBluetoothThermalPrinter(deviceId, value, std::forward<decltype(printerWidth)>(printerWidth));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> sendToUsbThermalPrinter(const std::string& value, double printerWidth, double chunkSize) override {
      auto __result = _swiftPart.sendToUsbThermalPrinter(value, std::forward<decltype(printerWidth)>(printerWidth), std::forward<decltype(chunkSize)>(chunkSize));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> feedPaper(const std::string& deviceId, double lines) override {
      auto __result = _swiftPart.feedPaper(deviceId, std::forward<decltype(lines)>(lines));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> cutPaper(const std::string& deviceId) override {
      auto __result = _swiftPart.cutPaper(deviceId);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }

  private:
    BlePrintAndScan::HybridBlePrinterSpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::bleprintandscan
