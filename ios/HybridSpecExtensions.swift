//
//  HybridSpecExtensions.swift
//  react-native-ble-print-and-scan
//
//  Created by Claude Code on 2025-01-05.
//

import Foundation
import NitroModules

// Extensions to provide dispose method for generated protocol types
extension HybridBleScannerSpec_protocol {
    func dispose() {
        // Default empty implementation for dispose
        // Concrete implementations can override this
    }
}

extension HybridBlePrinterSpec_protocol {
    func dispose() {
        // Default empty implementation for dispose
        // Concrete implementations can override this
    }
}